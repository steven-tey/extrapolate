{
  "_meta": {
    "template_version": 0
  },
  "fixtures": [
    {
      "name": "webhooks",
      "path": "/v1/webhook_endpoints",
      "method": "post",
      "params": {
        "enabled_events": ["product.created", "product.deleted", "product.updated", "price.created", "price.deleted", "price.updated", "checkout.session.completed"],
        "url": "https://extrapolate.app/api/webhooks/stripe"
      }
    }
  ]
}